RSpec.describe Watir::Screenshot::Stitch do
  it "has a version number" do
    expect(Watir::Screenshot::Stitch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
